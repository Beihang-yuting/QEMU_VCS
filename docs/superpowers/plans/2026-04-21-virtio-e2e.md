# QEMU Virtio 端到端测试实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 验证完整的 QEMU virtio-net 端到端数据通路：Guest VM 内 virtio-net 驱动发包 → cosim-pcie-rc → TCP transport → VCS 仿真 → virtqueue DMA 处理 → ETH 帧输出。

**Architecture:** QEMU (10.11.10.53) 运行 Buildroot Guest + cosim-pcie-rc (TCP 模式)，VCS (10.11.10.61) 运行 tb_top.sv 仿真。所有 MMIO/DMA 通过 TCP transport 跨机传输 TLP。Guest 内 virtio-net 驱动发包，VCS 侧 virtqueue_dma.c 处理 TX 队列并提取 ETH 帧。

**Tech Stack:** C (bridge/transport), SystemVerilog (VCS testbench), Buildroot (Guest Linux), QEMU v9.2.0, Bash (自动化脚本)

---

## 文件结构

| 文件 | 操作 | 职责 |
|------|------|------|
| `qemu-plugin/cosim_pcie_rc.h` | 修改 | 增加 TCP transport 属性字段 |
| `qemu-plugin/cosim_pcie_rc.c` | 修改 | 增加 TCP transport 属性定义 + realize() 中 TCP 初始化路径 |
| `bridge/eth/eth_mac_dpi.c` | 修改 | 增加 transport 感知：TCP 模式用 transport ETH 通道 |
| `vcs-tb/tb_top.sv` | 修改 | 增加 TCP plusargs 解析 + bridge_vcs_init_ex() 调用 |
| `scripts/setup_cosim_qemu.sh` | 修改 | 增加 Buildroot 构建步骤 + TCP 模式支持 |
| `scripts/run_cosim.sh` | 修改 | 增加 TCP 模式启动参数 |
| `scripts/buildroot_defconfig` | 新建 | Buildroot 配置片段 |
| `guest/S99cosim` | 新建 | Guest 网络自动配置 init 脚本 |
| `scripts/run_e2e_virtio.sh` | 新建 | 跨机端到端自动化启动 + 测试脚本 |

---

### Task 1: cosim_pcie_rc TCP Transport 属性

**Files:**
- Modify: `qemu-plugin/cosim_pcie_rc.h`
- Modify: `qemu-plugin/cosim_pcie_rc.c`

给 cosim-pcie-rc QEMU 设备增加 TCP transport 属性，使其可通过命令行参数选择 SHM 或 TCP 模式。

- [ ] **Step 1: 修改 cosim_pcie_rc.h 增加 TCP 字段**

在 `struct CosimPCIeRC` 中增加 TCP transport 相关字段：

```c
struct CosimPCIeRC {
    PCIDevice parent_obj;
    MemoryRegion bar0;
    char *shm_name, *sock_path;
    /* TCP transport properties */
    char *transport;     /* "shm" or "tcp" */
    char *remote_host;   /* TCP server address */
    uint32_t port_base;  /* TCP port base */
    uint32_t instance_id;/* instance ID for multi-device */
    void *bridge_ctx;
    void *irq_poller;
    void *msi_bh;
    uint32_t msi_queue[256];
    volatile int msi_queue_head, msi_queue_tail;
};
```

- [ ] **Step 2: 修改 cosim_pcie_rc.c 增加属性定义**

在 `cosim_properties[]` 数组中增加 TCP 属性：

```c
static Property cosim_properties[] = {
    DEFINE_PROP_STRING("shm_name", CosimPCIeRC, shm_name),
    DEFINE_PROP_STRING("sock_path", CosimPCIeRC, sock_path),
    DEFINE_PROP_STRING("transport", CosimPCIeRC, transport),
    DEFINE_PROP_STRING("remote_host", CosimPCIeRC, remote_host),
    DEFINE_PROP_UINT32("port_base", CosimPCIeRC, port_base, 9100),
    DEFINE_PROP_UINT32("instance_id", CosimPCIeRC, instance_id, 0),
    DEFINE_PROP_END_OF_LIST(),
};
```

- [ ] **Step 3: 修改 realize() 函数增加 TCP 初始化路径**

在 `cosim_pcie_rc_realize()` 中，检查 `transport` 属性决定初始化方式：

```c
/* 在 realize() 中，替换原有的 bridge_init() 调用 */
bridge_ctx_t *bctx;
if (s->transport && strcmp(s->transport, "tcp") == 0) {
    /* TCP transport mode - QEMU acts as server (listener) */
    transport_cfg_t cfg = {
        .transport   = "tcp",
        .listen_addr = "0.0.0.0",
        .remote_host = s->remote_host,
        .port_base   = s->port_base,
        .instance_id = s->instance_id,
        .is_server   = 1,
    };
    bctx = bridge_init_ex(&cfg);
    if (!bctx) {
        error_setg(errp, "cosim-pcie-rc: bridge_init_ex(tcp) failed");
        return;
    }
} else {
    /* Legacy SHM mode */
    bctx = bridge_init(s->shm_name, s->sock_path);
    if (!bctx) {
        error_setg(errp, "cosim-pcie-rc: bridge_init(shm) failed");
        return;
    }
}
s->bridge_ctx = bctx;
```

注意：TCP 模式下 QEMU 是 server（监听），VCS 是 client（连接）。`bridge_init_ex()` 内部会调用 `transport_create()` 创建 TCP transport 并等待 VCS 连接。

TCP 模式下跳过 `bridge_connect()` 调用（`bridge_init_ex()` 已完成连接握手）。`irq_poller` 在 TCP 模式下同样工作——它通过 transport 的 `recv_dma_req()` / `recv_msi()` 接口轮询。

需要增加头文件引用：

```c
#include "cosim_transport.h"  /* for transport_cfg_t */
```

- [ ] **Step 4: 处理 irq_poller 在 TCP 模式下的兼容**

确认 `irq_poller_start()` 在 TCP 模式下正确工作。当前 `irq_poller` 通过 `bridge_ctx_t` 内部的 `transport` 指针访问 DMA 和 MSI 通道，`bridge_init_ex()` 已正确设置了 `ctx->transport`。

检查 `irq_poller.c` 中是否有直接访问 SHM 的代码路径（如直接读 `shm.ctrl`），若有则需增加 transport 分支。

关键检查点：
- `irq_poller` 的 DMA 回调 `cosim_dma_cb` 使用 `cpu_physical_memory_read/write()`，不依赖 transport 类型——OK
- `irq_poller` 的 MSI 回调 `cosim_msi_cb` 只是入队 + 调度 BH——OK
- `irq_poller` 内部轮询循环需要确认：是从 SHM ring 还是 transport 接口读取？

如果 `irq_poller` 直接读 SHM ring，则需要修改为通过 `bridge_ctx_t->transport` 接口读取。如果它已经通过 bridge API（`bridge_poll_dma_req()` 等），则无需修改。

- [ ] **Step 5: 验证编译**

在 10.11.10.53 上编译验证：

```bash
cd /home/ubuntu/workspace/cosim-platform
# 重新编译 bridge library
cd build && cmake .. -DCMAKE_BUILD_TYPE=Debug && make -j$(nproc) && cd ..
# 重新编译 QEMU (setup_cosim_qemu.sh 已设置好环境)
cd qemu-9.2.0 && ninja -C build
```

预期：编译通过，无新增 warning。

- [ ] **Step 6: Commit**

```bash
git add qemu-plugin/cosim_pcie_rc.h qemu-plugin/cosim_pcie_rc.c
git commit -m "feat: add TCP transport properties to cosim-pcie-rc QEMU device"
```

---

### Task 2: eth_mac_dpi.c Transport 感知

**Files:**
- Modify: `bridge/eth/eth_mac_dpi.c`
- Modify: `bridge/vcs/bridge_vcs.c`

当前 `eth_mac_dpi.c` 的 `vcs_eth_mac_send_raw()` / `vcs_eth_mac_recv_raw()` 只通过 ETH SHM 通道工作。在 TCP 模式下，需要通过 transport 的 ETH 通道发送/接收帧。

- [ ] **Step 1: 增加 transport 指针存储**

在 `eth_mac_dpi.c` 中增加全局 transport 指针和设置函数：

```c
#include "cosim_transport.h"

static eth_port_t g_port;
static int g_opened = 0;
static cosim_transport_t *g_transport = NULL;  /* non-NULL = TCP mode */

void vcs_eth_mac_set_transport(void *transport_ptr)
{
    g_transport = (cosim_transport_t *)transport_ptr;
    printf("[eth_mac_dpi] transport set, mode=%s\n",
           g_transport ? "TCP" : "SHM");
}
```

- [ ] **Step 2: 修改 vcs_eth_mac_send_raw() 增加 transport 分支**

```c
int vcs_eth_mac_send_raw(const uint8_t *data, int len)
{
    if (g_transport) {
        /* TCP mode: send via transport ETH channel */
        eth_frame_t f;
        memset(&f, 0, sizeof(f));
        if (len > (int)ETH_FRAME_MAX_DATA) len = (int)ETH_FRAME_MAX_DATA;
        memcpy(f.data, data, len);
        f.len = (uint16_t)len;
        f.seq = 0;
        f.timestamp_ns = 0;
        return g_transport->send_eth(g_transport, &f);
    }
    /* Legacy SHM mode */
    if (!g_opened) return -1;
    eth_frame_t f;
    memset(&f, 0, sizeof(f));
    if (len > (int)ETH_FRAME_MAX_DATA) len = (int)ETH_FRAME_MAX_DATA;
    memcpy(f.data, data, len);
    f.len = (uint16_t)len;
    return eth_port_send(&g_port, &f, 0);
}
```

- [ ] **Step 3: 修改 vcs_eth_mac_recv_raw() 增加 transport 分支**

```c
int vcs_eth_mac_recv_raw(uint8_t *data, int max_len)
{
    if (g_transport) {
        /* TCP mode: recv via transport ETH channel (non-blocking) */
        eth_frame_t f;
        int rc = g_transport->recv_eth(g_transport, &f, 0);
        if (rc != 0) return 0;  /* no frame available */
        int copy_len = (f.len < max_len) ? f.len : max_len;
        memcpy(data, f.data, copy_len);
        return copy_len;
    }
    /* Legacy SHM mode */
    if (!g_opened) return 0;
    eth_frame_t f;
    int rc = eth_port_recv(&g_port, &f, 0);
    if (rc != 0) return 0;
    int copy_len = (f.len < max_len) ? f.len : max_len;
    memcpy(data, f.data, copy_len);
    eth_port_tx_complete(&g_port);
    return copy_len;
}
```

- [ ] **Step 4: 在 bridge_vcs_init_ex() 末尾设置 transport**

在 `bridge/vcs/bridge_vcs.c` 的 `bridge_vcs_init_ex()` 成功后，调用 `vcs_eth_mac_set_transport()`：

```c
/* bridge_vcs_init_ex() 末尾，g_transport 设置成功后 */
if (g_transport) {
    extern void vcs_eth_mac_set_transport(void *);
    vcs_eth_mac_set_transport(g_transport);
}
```

这样 eth_mac_dpi.c 自动获得 transport 指针，无需 SV 层参与。

- [ ] **Step 5: 本地编译验证**

```bash
cd /home/ubuntu/ryan/software/cosim-platform
cmake -B build -DCMAKE_BUILD_TYPE=Debug && cmake --build build -j$(nproc)
```

预期：编译通过。

- [ ] **Step 6: Commit**

```bash
git add bridge/eth/eth_mac_dpi.c bridge/vcs/bridge_vcs.c
git commit -m "feat: add transport awareness to eth_mac_dpi for TCP mode"
```

---

### Task 3: tb_top.sv TCP 模式初始化

**Files:**
- Modify: `vcs-tb/tb_top.sv`
- Modify: `bridge/vcs/bridge_vcs.sv`

当前 tb_top.sv 只解析 SHM 相关 plusargs，需要增加 TCP 模式 plusargs 并调用 `bridge_vcs_init_ex()`。

- [ ] **Step 1: 在 bridge_vcs.sv 中增加 DPI import**

在 `bridge/vcs/bridge_vcs.sv` 中增加 `bridge_vcs_init_ex` 的 DPI 声明：

```systemverilog
import "DPI-C" function int bridge_vcs_init_ex(
    input string transport_type,
    input string shm_name,
    input string sock_path,
    input string remote_host,
    input int    port_base,
    input int    instance_id
);
```

- [ ] **Step 2: 增加 TCP plusargs 解析**

在 tb_top.sv 的 `initial begin` 块中，在现有 plusargs 之后增加：

```systemverilog
    // TCP transport plusargs
    string transport_type;
    string listen_addr;
    string remote_host;
    int    port_base;
    int    instance_id;
    int    use_tcp;

    // 现有 plusargs 保持不变
    if (!$value$plusargs("SHM_NAME=%s", shm_name))   shm_name = "/cosim0";
    if (!$value$plusargs("SOCK_PATH=%s", sock_path))  sock_path = "/tmp/cosim.sock";
    // ...

    // TCP transport plusargs
    use_tcp = 0;
    if ($value$plusargs("transport=%s", transport_type)) begin
        if (transport_type == "tcp") use_tcp = 1;
    end
    if (!$value$plusargs("LISTEN=%s", listen_addr))      listen_addr = "0.0.0.0";
    if (!$value$plusargs("REMOTE_HOST=%s", remote_host)) remote_host = "";
    if (!$value$plusargs("PORT_BASE=%d", port_base))     port_base = 9100;
    if (!$value$plusargs("INSTANCE_ID=%d", instance_id)) instance_id = 0;
```

- [ ] **Step 3: 替换 bridge 初始化调用**

将现有的 `bridge_vcs_init()` 调用替换为条件分支：

```systemverilog
    // Bridge initialization
    if (use_tcp) begin
        int rc;
        $display("[tb_top] TCP mode: remote_host=%s port_base=%0d instance_id=%0d",
                 remote_host, port_base, instance_id);
        rc = bridge_vcs_init_ex("tcp", "", "",
                                 remote_host, port_base, instance_id);
        if (rc != 0) begin
            $display("[tb_top] ERROR: bridge_vcs_init_ex(tcp) failed");
            $finish;
        end
        // TCP mode: skip ETH SHM init (transport handles ETH via bridge_vcs_init_ex)
        $display("[tb_top] TCP transport initialized, ETH via transport");
    end else begin
        bridge_vcs_init(shm_name, sock_path);
        vcs_eth_mac_init_dpi(eth_shm_name, eth_role, eth_create);
        $display("[tb_top] SHM transport initialized");
    end
```

- [ ] **Step 4: 更新 SIM_TIMEOUT_MS 默认值**

```systemverilog
    if (!$value$plusargs("SIM_TIMEOUT_MS=%d", sim_timeout_ms)) begin
        sim_timeout_ms = use_tcp ? 60000 : 10000;  // TCP mode default 60s
    end
```

- [ ] **Step 5: Commit**

```bash
git add vcs-tb/tb_top.sv bridge/vcs/bridge_vcs.sv
git commit -m "feat: add TCP transport mode support to tb_top.sv"
```

---

### Task 4: Buildroot Guest 镜像构建

**Files:**
- Create: `scripts/buildroot_defconfig`
- Create: `guest/S99cosim`

在 10.11.10.53 上构建最小化 x86_64 Linux Guest 镜像。

- [ ] **Step 1: 创建 Buildroot 配置片段**

创建 `scripts/buildroot_defconfig`：

```
# Buildroot defconfig fragment for cosim virtio guest
# Usage: make qemu_x86_64_defconfig
#        cat scripts/buildroot_defconfig >> .config
#        make olddefconfig && make

# Packages
BR2_PACKAGE_IPERF3=y
BR2_PACKAGE_IPROUTE2=y

# Filesystem
BR2_TARGET_ROOTFS_EXT2=y
BR2_TARGET_ROOTFS_EXT2_4=y
BR2_TARGET_ROOTFS_EXT2_SIZE="64M"

# Console
BR2_TARGET_GENERIC_GETTY_PORT="ttyS0"
BR2_TARGET_GENERIC_GETTY_BAUDRATE_115200=y
```

- [ ] **Step 2: 创建 Guest 网络配置脚本**

创建 `guest/S99cosim`：

```bash
#!/bin/sh
# S99cosim - Auto-configure cosim virtio-net interface
# Install to: /etc/init.d/S99cosim in Buildroot overlay

case "$1" in
    start)
        echo "Configuring cosim virtio-net..."
        for i in $(seq 1 10); do
            if ip link show eth0 > /dev/null 2>&1; then
                break
            fi
            sleep 1
        done
        if ip link show eth0 > /dev/null 2>&1; then
            ip addr add 10.0.0.2/24 dev eth0
            ip link set eth0 up
            echo "eth0 configured: 10.0.0.2/24"
        else
            echo "WARNING: eth0 not found"
        fi
        ;;
    stop)
        ip link set eth0 down 2>/dev/null
        ;;
    *)
        echo "Usage: $0 {start|stop}"
        exit 1
        ;;
esac
```

- [ ] **Step 3: 在 10.11.10.53 上构建 Buildroot**

SSH 到 53，执行构建：

```bash
ssh ubuntu@10.11.10.53
cd /home/ubuntu/workspace
wget https://buildroot.org/downloads/buildroot-2024.02.tar.gz
tar xf buildroot-2024.02.tar.gz
cd buildroot-2024.02

# 基础配置
make qemu_x86_64_defconfig

# 应用 cosim 配置
cat /home/ubuntu/workspace/cosim-platform/scripts/buildroot_defconfig >> .config
make olddefconfig

# 设置 overlay
mkdir -p board/cosim/overlay/etc/init.d
cp /home/ubuntu/workspace/cosim-platform/guest/S99cosim board/cosim/overlay/etc/init.d/S99cosim
chmod +x board/cosim/overlay/etc/init.d/S99cosim
echo 'BR2_ROOTFS_OVERLAY="board/cosim/overlay"' >> .config
make olddefconfig

# 构建
make -j$(nproc)
```

验证输出：
```bash
ls -la output/images/bzImage          # ~8MB
ls -la output/images/rootfs.ext2      # ~64MB
```

- [ ] **Step 4: 复制镜像到 cosim-platform 目录**

```bash
mkdir -p /home/ubuntu/workspace/cosim-platform/guest
cp output/images/bzImage /home/ubuntu/workspace/cosim-platform/guest/
cp output/images/rootfs.ext2 /home/ubuntu/workspace/cosim-platform/guest/rootfs.ext4
```

- [ ] **Step 5: 快速验证 Guest 可启动**

```bash
qemu-system-x86_64 \
    -machine q35 -nographic -m 512M \
    -kernel /home/ubuntu/workspace/cosim-platform/guest/bzImage \
    -drive file=/home/ubuntu/workspace/cosim-platform/guest/rootfs.ext4,format=raw,if=virtio \
    -append "root=/dev/vda console=ttyS0 nokaslr" \
    -netdev user,id=net0 -device virtio-net-pci,netdev=net0
```

预期：Linux 启动，出现 login 提示。登录 root（无密码），`ip link` 确认 eth0 存在。Ctrl-A X 退出。

- [ ] **Step 6: Commit**

```bash
git add scripts/buildroot_defconfig guest/S99cosim
git commit -m "feat: add Buildroot config and guest init script for virtio e2e"
```

---

### Task 5: run_cosim.sh TCP 模式更新

**Files:**
- Modify: `scripts/run_cosim.sh`

更新 QEMU 启动脚本支持 TCP 模式参数和 Buildroot rootfs。

- [ ] **Step 1: 增加 TCP 模式参数**

在 `scripts/run_cosim.sh` 中，现有参数之后增加：

```bash
TRANSPORT="${TRANSPORT:-shm}"
REMOTE_HOST="${REMOTE_HOST:-}"
PORT_BASE="${PORT_BASE:-9100}"
INSTANCE_ID="${INSTANCE_ID:-0}"
GUEST_ROOTFS="${GUEST_ROOTFS:-}"

# Device string
if [ "$TRANSPORT" = "tcp" ]; then
    COSIM_DEVICE="cosim-pcie-rc,transport=tcp,remote_host=${REMOTE_HOST},port_base=${PORT_BASE},instance_id=${INSTANCE_ID}"
else
    COSIM_DEVICE="cosim-pcie-rc,shm_name=${SHM_NAME},sock_path=${SOCK_PATH}"
fi

# Rootfs args
if [ -n "$GUEST_ROOTFS" ] && [ -f "$GUEST_ROOTFS" ]; then
    ROOTFS_ARGS="-drive file=${GUEST_ROOTFS},format=raw,if=virtio"
    KERNEL_ARGS="root=/dev/vda console=ttyS0 nokaslr"
else
    KERNEL_ARGS="console=ttyS0 nokaslr"
fi
```

- [ ] **Step 2: 更新 QEMU 启动命令使用新变量**

```bash
${QEMU_BIN} \
    -machine q35 ${ACCEL} \
    -m ${MEMORY} \
    -nographic \
    -device "${COSIM_DEVICE}" \
    -kernel "${GUEST_KERNEL}" \
    ${ROOTFS_ARGS} \
    -append "${KERNEL_ARGS}" \
    ${GDB_ARGS}
```

- [ ] **Step 3: Commit**

```bash
git add scripts/run_cosim.sh
git commit -m "feat: add TCP mode and rootfs support to run_cosim.sh"
```

---

### Task 6: 跨机端到端启动脚本

**Files:**
- Create: `scripts/run_e2e_virtio.sh`

自动化跨机启动流程：SSH 到 VCS 机器启动仿真 → 在 QEMU 机器启动 Guest → 等待 Guest 就绪 → 执行测试。

- [ ] **Step 1: 创建 run_e2e_virtio.sh**

```bash
#!/bin/bash
# run_e2e_virtio.sh - QEMU virtio E2E test automation
#
# Deployment:
#   QEMU (10.11.10.53): cosim-pcie-rc TCP server + Buildroot Guest
#   VCS  (10.11.10.61): tb_top.sv simulation, TCP client
#
# Usage (run on 10.11.10.53):
#   ./scripts/run_e2e_virtio.sh [--phase 1|2|3|all] [--timeout 60]

set -euo pipefail

# === Configuration ===
VCS_HOST="10.11.10.61"
VCS_PORT=2222
VCS_USER="ryan"
VCS_PASS="Ryan@2025"

PROJECT_DIR="/home/ubuntu/workspace/cosim-platform"
PROJECT_DIR_VCS="/home/ryan/cosim-platform"

PORT_BASE=9100
INSTANCE_ID=0
SIM_TIMEOUT_MS=60000

GUEST_KERNEL="${PROJECT_DIR}/guest/bzImage"
GUEST_ROOTFS="${PROJECT_DIR}/guest/rootfs.ext4"

PHASE="all"
TIMEOUT=60

# Parse args
while [ $# -gt 0 ]; do
    case "$1" in
        --phase) PHASE="$2"; shift 2 ;;
        --timeout) TIMEOUT="$2"; shift 2 ;;
        *) echo "Unknown arg: $1"; exit 1 ;;
    esac
done

VCS_SSH="sshpass -p '${VCS_PASS}' ssh -o StrictHostKeyChecking=no -p ${VCS_PORT} ${VCS_USER}@${VCS_HOST}"
QEMU_LOG="/tmp/qemu_e2e.log"

echo "=== QEMU Virtio E2E Test ==="
echo "Phase: ${PHASE}, Timeout: ${TIMEOUT}s"
echo ""

# === Step 1: Start VCS simulation ===
echo "[1/4] Starting VCS simulation on ${VCS_HOST}..."
eval ${VCS_SSH} "pkill -f simv_legacy || true" 2>/dev/null || true

eval ${VCS_SSH} "cd ${PROJECT_DIR_VCS} && \
    nohup ./build/simv_legacy \
        +transport=tcp \
        +REMOTE_HOST=10.11.10.53 \
        +PORT_BASE=${PORT_BASE} \
        +INSTANCE_ID=${INSTANCE_ID} \
        +SIM_TIMEOUT_MS=${SIM_TIMEOUT_MS} \
        > /tmp/vcs_e2e.log 2>&1 &"

sleep 3
echo "[1/4] VCS simulation starting..."

# === Step 2: Start QEMU ===
echo "[2/4] Starting QEMU (TCP server mode)..."

TRANSPORT=tcp \
PORT_BASE=${PORT_BASE} \
INSTANCE_ID=${INSTANCE_ID} \
GUEST_KERNEL="${GUEST_KERNEL}" \
GUEST_ROOTFS="${GUEST_ROOTFS}" \
MEMORY=512M \
    ${PROJECT_DIR}/scripts/run_cosim.sh > ${QEMU_LOG} 2>&1 &
QEMU_PID=$!

echo "[2/4] QEMU started (PID: ${QEMU_PID})"

# === Step 3: Wait for Guest boot ===
echo "[3/4] Waiting for Guest to boot..."

BOOT_TIMEOUT=30
BOOTED=0
for i in $(seq 1 ${BOOT_TIMEOUT}); do
    if grep -q "login:" ${QEMU_LOG} 2>/dev/null; then
        echo "[3/4] Guest booted (${i}s)"
        BOOTED=1
        break
    fi
    if ! kill -0 ${QEMU_PID} 2>/dev/null; then
        echo "ERROR: QEMU exited"
        cat ${QEMU_LOG}
        exit 1
    fi
    sleep 1
done

if [ "$BOOTED" = "0" ]; then
    echo "ERROR: Boot timeout (${BOOT_TIMEOUT}s)"
    tail -50 ${QEMU_LOG}
    kill ${QEMU_PID} 2>/dev/null || true
    exit 1
fi

# === Step 4: Run tests ===
echo "[4/4] Running tests..."

# Phase 1: Device identification
if [ "$PHASE" = "1" ] || [ "$PHASE" = "all" ]; then
    echo ""
    echo "=== Phase 1: Device Identification ==="
    echo "Check Guest serial console for: lspci, dmesg | grep virtio, ip link"
    echo "Check VCS log for: feature negotiation, queue setup, DRIVER_OK"
fi

# Phase 2: TX packets
if [ "$PHASE" = "2" ] || [ "$PHASE" = "all" ]; then
    echo ""
    echo "=== Phase 2: TX Packet Verification ==="
    echo "In Guest: ip addr add 10.0.0.2/24 dev eth0; ip link set eth0 up; ping -c 5 10.0.0.1"
    echo "Check VCS log for: notify_valid, vcs_vq_process_tx() > 0, ETH frame hex dump"
fi

# Phase 3: Throughput
if [ "$PHASE" = "3" ] || [ "$PHASE" = "all" ]; then
    echo ""
    echo "=== Phase 3: Throughput Test ==="
    echo "In Guest: iperf3 -c 10.0.0.1 -u -b 100M -t 10 --no-delay"
    echo "Check VCS log for: per-second frame count, 10s stability, zero corruption"
fi

echo ""
echo "=== Logs ==="
echo "QEMU: ${QEMU_LOG}"
echo "VCS:  ssh -p ${VCS_PORT} ${VCS_USER}@${VCS_HOST} cat /tmp/vcs_e2e.log"
echo ""
echo "Press Ctrl-C to stop, or wait for timeout"
wait ${QEMU_PID} || true
```

- [ ] **Step 2: 设置可执行权限并 commit**

```bash
chmod +x scripts/run_e2e_virtio.sh
git add scripts/run_e2e_virtio.sh
git commit -m "feat: add cross-machine e2e virtio test launch script"
```

---

### Task 7: 远程编译部署（QEMU 侧 10.11.10.53）

**Files:** 无新文件，操作远程机器

- [ ] **Step 1: 同步代码到 53**

```bash
cd /home/ubuntu/ryan/software/cosim-platform
rsync -avz --exclude build/ --exclude .git/ \
    ./ ubuntu@10.11.10.53:/home/ubuntu/workspace/cosim-platform/
```

- [ ] **Step 2: 重编 bridge library**

```bash
sshpass -p '123' ssh ubuntu@10.11.10.53 \
    "cd /home/ubuntu/workspace/cosim-platform && \
     cmake -B build -DCMAKE_BUILD_TYPE=Debug && \
     cmake --build build -j\$(nproc)"
```

- [ ] **Step 3: 更新并重编 QEMU**

```bash
sshpass -p '123' ssh ubuntu@10.11.10.53 \
    "cp /home/ubuntu/workspace/cosim-platform/qemu-plugin/cosim_pcie_rc.c \
        /home/ubuntu/workspace/qemu-9.2.0/hw/net/ && \
     cp /home/ubuntu/workspace/cosim-platform/qemu-plugin/cosim_pcie_rc.h \
        /home/ubuntu/workspace/qemu-9.2.0/hw/net/ && \
     cd /home/ubuntu/workspace/qemu-9.2.0 && ninja -C build"
```

- [ ] **Step 4: 验证 QEMU 设备属性**

```bash
sshpass -p '123' ssh ubuntu@10.11.10.53 \
    "/home/ubuntu/workspace/qemu-9.2.0/build/qemu-system-x86_64 -device cosim-pcie-rc,help"
```

预期输出包含 `transport`, `remote_host`, `port_base`, `instance_id`。

---

### Task 8: 远程编译部署（VCS 侧 10.11.10.61）

**Files:** 无新文件，操作远程机器

- [ ] **Step 1: 同步代码到 61**

```bash
cd /home/ubuntu/ryan/software/cosim-platform
rsync -avz --exclude build/ --exclude .git/ \
    -e "ssh -p 2222" \
    ./ ryan@10.11.10.61:~/cosim-platform/
```

- [ ] **Step 2: 编译 simv_legacy**

```bash
sshpass -p 'Ryan@2025' ssh -o StrictHostKeyChecking=no -p 2222 ryan@10.11.10.61 \
    "cd ~/cosim-platform && make vcs-legacy"
```

注意 GCC 4.8 兼容性：
- `stdatomic.h` → 使用 `__sync_*` builtins
- 确保 VCS_CFLAGS 包含 `-std=c99`

- [ ] **Step 3: 快速验证 simv TCP 模式**

```bash
sshpass -p 'Ryan@2025' ssh -o StrictHostKeyChecking=no -p 2222 ryan@10.11.10.61 \
    "cd ~/cosim-platform && timeout 5 ./build/simv_legacy \
        +transport=tcp +REMOTE_HOST=10.11.10.53 \
        +PORT_BASE=9100 +INSTANCE_ID=0 +SIM_TIMEOUT_MS=5000 || true"
```

预期：simv 启动，打印 TCP 连接日志，5 秒后超时退出。

---

### Task 9: 端到端测试执行

**Files:** 无新文件

按三个阶段执行验证。

- [ ] **Step 1: 阶段 1 - 设备识别验证**

终端 1（53, QEMU TCP server）:
```bash
cd /home/ubuntu/workspace/cosim-platform
TRANSPORT=tcp PORT_BASE=9100 INSTANCE_ID=0 \
GUEST_KERNEL=guest/bzImage GUEST_ROOTFS=guest/rootfs.ext4 \
MEMORY=512M scripts/run_cosim.sh
```

终端 2（61, VCS TCP client）:
```bash
cd ~/cosim-platform
./build/simv_legacy \
    +transport=tcp +REMOTE_HOST=10.11.10.53 \
    +PORT_BASE=9100 +INSTANCE_ID=0 +SIM_TIMEOUT_MS=60000
```

Guest 启动后登录 root，执行：
```bash
lspci                    # 应看到 1af4:1041
dmesg | grep virtio      # 应看到驱动加载
ip link show eth0        # 应看到接口
```

VCS 日志验证：feature negotiation TLP、queue setup TLP、DRIVER_OK。

- [ ] **Step 2: 阶段 2 - TX 报文验证**

Guest 中执行：
```bash
ip addr add 10.0.0.2/24 dev eth0
ip link set eth0 up
ping -c 5 10.0.0.1
```

VCS 日志验证：notify_valid 触发、`vcs_vq_process_tx()` > 0、ETH 帧 hex dump。

- [ ] **Step 3: 阶段 3 - 吞吐量测试**

Guest 中执行：
```bash
iperf3 -c 10.0.0.1 -u -b 100M -t 10 --no-delay
```

验收标准：
- 持续 10 秒稳定运行
- 吞吐量 > 1 Kpps
- 零帧损坏
